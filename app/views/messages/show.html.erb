<%= render @message %>

<div>
  <%= link_to "Edit this message", edit_discussion_message_path(@discussion, @message) %> |
  <%= link_to "Back to messages", discussion_messages_path(@discussion) %>

  <%= button_to "Destroy this message", discussion_message_path(@discussion, @message), method: :delete, data: { confirm: "Are you sure?" }  %>
</div>
